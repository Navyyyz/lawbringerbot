module.exports = message => {
  const member = message.mentions.members.first()

  if (!member) {
    return message.reply(`Not if I fuck you first :wink:`)
  }

  if (!member) {
    return message.reply(`Not if I fuck you first :smirk:`)
  }
}